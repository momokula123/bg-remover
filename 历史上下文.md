# 历史上下文

## 2026-06-24 便携包打包

- 用户要求将 bg-remover 项目打包为便携包（venv + bat）
- 创建了 `build_portable.ps1` 打包脚本
- 打包成功，输出目录: `bg-remover-portable/`，总大小约 2503 MB
- 便携包结构:
  - `start.bat` - 启动脚本（用户双击运行）
  - `venv/` - 内置 Python 虚拟环境及所有依赖
  - `models/` - ONNX 模型文件 (birefnet-massive, bria-rmbg)
  - `templates/` - HTML 模板
  - `app.py` - 主程序
  - `README.txt` - 使用说明
  - `inputs/` `outputs/` - 输入输出目录
- 用户分发方式: 将 `bg-remover-portable` 文件夹压缩为 zip

## 2026-06-24 GitHub Release 上传

- 将便携包 zip (1.76GB) 上传到 GitHub Release
- 创建 Release v1.0.0: https://github.com/momokula123/bg-remover/releases/tag/v1.0.0
- 同时推送了 build_portable.ps1 和 .gitignore 更新到 master 分支

## 2026-06-24 便携包修复 - 增量补丁

- 问题: venv 缺少 python310.dll 和标准库，pyvenv.cfg 的 home 指向本地路径，在其他机器无法运行
- 修复方案: 新增 `python/` 目录存放完整 Python 运行时，通过 sitecustomize.py 指向 venv 的 site-packages
- 修改 start.bat: 改用 `python\python.exe` 替代 `venv\Scripts\python.exe`
- 生成增量补丁 `bg-remover-patch.zip` (33.6 MB)，已上传到 Release v1.0.0
- 补丁包含: python运行时 + MSVC运行时DLL(vcruntime140/msvcp140等) + DLLs目录 + start.bat + pyvenv.cfg
- 使用方式: 解压补丁覆盖到 bg-remover-portable 目录即可

## 2026-06-25 清理本地便携包目录

- 用户要求删除 `A:\3\codex\bg-remover\bg-remover-portable`
- 已检查占用情况，未发现长期运行的 bg-remover-portable 进程
- 已删除本地 `bg-remover-portable` 文件夹
